.c-container--md
  = form_for @selection, html: { class: "c-form" } do |f|
    - if @selection.errors.any?
      = render "shared/errors", target: @selection
    = render "fields", f: f
    = f.submit "Draft Player",
      class: "c-button c-button--positive"
