.grid.grid-cols-2.gap-8
  .c-panel
    .form-group.form-group--required
      = f.label :name, "Draft Name",
        class: "c-form__label"
      = f.text_field :name
    .form-group.form-group--required
      = f.label :round_count, "Number of Rounds",
        class: "c-form__label"
      = f.number_field :round_count
    .form-group.form-group--required
      = f.label :selection_seconds, "Time per Selection",
        class: "c-form__label"
      = f.number_field :selection_seconds
  .c-panel
    .form-group.form-group--required
      = f.label :user_count, "Number of Participants",
        class: "c-form__label"
      = f.number_field :user_count
      - if f.object.present?
        p
          strong Users currently paired
        - if f.object.pairings.any?
          ul
            - f.object.pairings.each do |pairing|
              li
                small = pairing.user.email
                = link_to "Remove",
                  draft_pairing_path(f.object, pairing),
                  method: :delete, data: { turbo_method: :delete }
